# Node.js dependencies
node_modules/

# Next.js build output (legacy -- removed in build-order step 7)
.next/
out/

# Astro build output + local cache
dist/
.astro/

# Vitest coverage report
coverage/

# Production build
build/

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local environment files
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# Misc
.DS_Store
*.pem

# Working artefacts from the NAS scan -- not committed (spec D7 / nas-scan-spec.md)
# `npm run data` regenerates these locally from the NAS
data/raw/

# Legacy Next.js generated data (file still tracked from before .gitignore;
# git rm --cached at build-order step 7)
public/orynt3d-data.json
public/images/

# Build artifacts
build-log.txt
.build-cache.json
