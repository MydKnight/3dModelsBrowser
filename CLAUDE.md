# CLAUDE.md

This file provides guidance to Claude Code when working with this repository.

## Project Overview

3dModelsBrowser is a static web app that serves as a searchable gallery for a personal 3D model collection. It reads metadata produced by Orynt3D (the desktop cataloguing app) and makes it browsable from anywhere via Netlify.

**Personal tool.** No model data or images in git -- `src/data/*.json` +
`public/thumbnails|detail/` are generated by `npm run data` from the NAS and
committed as a snapshot; Netlify builds from that snapshot with no NAS access.

## Status: v2.0 Astro rewrite -- PARKED (`feat/astro-rewrite`), container work active (`feat/nas-data-container`)

**v2.0 built and verified.** `feat/astro-rewrite` @ `16b9acf` = Astro rewrite
(steps 1-7) + the NAS-data container + the real **3,882-model snapshot** the
container produced on the QNAP. `astro build` = 3,883 pages in ~18s;
`index.html` 154 KB gzip + ~16 KB island JS; local preview confirmed fully
functional (filter, sort, AND/OR, live counts, windowed grid, detail pages).
138 tests.

The container (`docs/nas-container-spec.md`, Locked) ran end-to-end on the QNAP:
scan ~58x faster than VPN, thumbnails ~0.2 s/model, pushed its own
`chore(data): snapshot` commit. `feat/nas-data-container` was the vehicle and is
now == `feat/astro-rewrite`.

**Remaining:** `/code-review` -> merge `feat/astro-rewrite` to `main` -> repoint
production Netlify -> delete both feat branches -> set the QNAP `.env`
`TARGET_BRANCH=main`.

**Known gaps (container handles on the next refresh):**
- "The Trench - Crustaceans of the Deep" resolves as a subscription (27 models) --
  it's a misplaced Archvillain release; move it under `Archvillain Games/` on the NAS.
- 194 models get the placeholder thumbnail (no render on the NAS). Some are
  genuine (Grinning God, Unchained Immortals); check whether the Rescale Welcome
  Pack ones actually have images `pickSourceImage` is missing.

**Phase 2 backlog:** `docs/astro-rewrite-spec.md` -> Phase 2 backlog. Visual
polish, detail-page CLS, scroll-restore timing, `scan-nas` busy-wait backoff,
thumbnail source manifest, incremental scan, container cron, WebP->R2. The
2026-09-02 code review's non-blocking findings (#3/#4/#5) are captured there;
#1/#2/#6 were fixed pre-merge.

Full Astro rewrite of a Next.js app that didn't scale to the real collection
(~4k models, +100/month): unvirtualized grid, whole-metadata payload, and a
`STATIC_DATA_PLACEHOLDER` env-var data hack that blocked Netlify CI.

**Center of gravity: filter/tag island performance at 4k+ models** -- bitset
result sets + live per-facet counts + a windowed grid. Build time / bundle size
are not the concern.

Specs:
- **`docs/astro-rewrite-spec.md`** (Locked) -- the whole rewrite.
- **`docs/nas-scan-spec.md`** (Locked) -- the NAS scanner that replaced `extract-model-data.cjs`.
- **`docs/nas-container-spec.md`** (Draft) -- run the data pipeline in a Docker
  container ON the QNAP instead of over VPN. A **separate feature**
  (`feat/nas-data-container`), branched off `main` *after* astro-rewrite merges.
  Not started.

**Build-order status** (see astro-rewrite-spec.md -> Build order):
- Steps 1-7: **code done**, 137 tests, `astro build` = 945 pages in ~5s.
  scan-nas + make-thumbnails hardened against SMB/VPN transient failures.
- First real full scan (2026-09-02): 3,859 models (old script: 944). Surfaced +
  fixed: imageless models -> placeholder, config-less STL leaves, misplaced
  "The Trench" folder (owner to move under `Archvillain Games/`).
- **Pending owner action:** a clean `npm run data` (scan -> thumbnails ->
  build-filter-index) against a quiet NAS, then commit `src/data/*.json` +
  `public/thumbnails|detail/` and un-gitignore the two json files. If the VPN
  keeps losing, merge with the 944-model bootstrap as an interim snapshot and
  let `feat/nas-data-container`'s first run produce the real one.
- Step 8: Netlify branch deploy (gated on data). Step 9: `/code-review`, merge.

## Where This Lives in the Pipeline

```
Raw subscription downloads
        ↓
  orynt3d-pipeline (reorganizes files into Orynt3D folder structure)
        ↓
  Orynt3D desktop app (scans, previews, writes config.orynt3d per model)
        ↓
  3dModelsBrowser ← this project
  (scan-nas.mjs reads config.orynt3d files → data/raw/models.json → build-filter-index.mjs → Astro gallery → Netlify)
```

## Architecture

```
src/
  pages/
    index.astro          # gallery shell; hosts the filter island
    m/[id].astro          # per-model detail page (getStaticPaths -> detail-paths.ts)
  layouts/Base.astro      # shared shell + <ClientRouter /> (View Transitions)
  components/GalleryIsland.tsx  # the single client:load island (panel + windowed grid)
  lib/                    # pure, unit-tested logic:
    filter-engine.ts      #   bitsets + facet counts (D4)
    use-gallery.ts        #   island state: FilterState + engine + URL sync
    url-state.ts          #   FilterState <-> query string (D8)
    grid-layout.ts        #   column/row/scroll geometry for the windowed grid
    detail-paths.ts       #   getStaticPaths for /m/[id]
  data/
    filter-index.example.json   # COMMITTED shape doc (hand-written)
    filter-index.json / details.json  # generated; gitignored until the real npm run data
scripts/
  scan-nas.mjs            # walks NAS config.orynt3d -> data/raw/models.json (docs/nas-scan-spec.md)
  make-thumbnails.mjs     # sharp: sourceImage -> public/thumbnails|detail/<id>.webp (400/900px)
  build-filter-index.mjs  # data/raw/models.json -> src/data/*.json  (no NAS; buildIndex() is pure)
  dev-bootstrap-raw.mjs   # THROWAWAY: legacy snapshot -> data/raw/models.bootstrap.json
  lib/ { recency.cjs, model-resolve.mjs, thumbnail-paths.cjs }
data/raw/                 # GITIGNORED working artefacts
tests/fixtures/           # build-nas-fixture.mjs (temp NAS tree), raw-models.mjs
public/thumbnails/ detail/ # COMMITTED WebP renditions (once npm run data has run)
example configs/           # reference config.orynt3d samples
```

## Tech Stack

- **Astro 7 (static)** + one **Preact + `@preact/signals`** island (search/filter/grid)
- Row windowing hand-rolled over `grid-layout.ts` (not `@tanstack/virtual` -- uniform heights)
- **`sharp`** (local only) for WebP thumbnails; images referenced by plain `<img src>` (no `astro:assets`)
- **Vitest** + `@testing-library/preact`; `type: "module"`, legacy scripts are `.cjs`
- **Netlify** -- `npm run build` = `astro build` only, reads the committed snapshot, no NAS

## Pipeline

```
NAS  -> scan-nas.mjs        -> data/raw/models.json          (gitignored)
     -> make-thumbnails.mjs -> public/thumbnails|detail/*.webp (committed)
     -> build-filter-index.mjs -> src/data/{filter-index,details}.json (committed)
     -> astro build         (no NAS)
```

`npm run data` runs the first three; **commit the outputs** -- that is the snapshot Netlify builds from.

## Current State

Steps 1-7 code-complete on `feat/astro-rewrite`; 133 tests; `astro build` = 945
pages (1 gallery + 944 detail from the bootstrap) in ~5s; `astro dev` serves it.
The Next.js app is fully removed. `npm audit` clean.

Frontend currently runs on a **944-model bootstrap** (`dev-bootstrap-raw.mjs`
converts the old `public/orynt3d-data.json`) because the real full NAS scan
couldn't finish in-session (variable SMB latency). Verified against a real NAS
subset: Witchsong Miniatures 0 -> 19 models, correct fields, real fs birthtime.

## Known Gaps

- **Real data not generated yet.** Owner runs `npm run data` (see Next Actions).
- Bootstrap data is stale: old folder layout, doubled-space names, missing the
  subscriptions the old script dropped. Structurally identical to real output,
  so nothing built against it needs rework.
- No `.env.example` for `ORYNT3D_DIR` (hardcoded default in `scan-nas.mjs`).
- Scroll perf on the real full snapshot / a throttled mobile profile: unverified.
- `public/images/` (588 MB legacy PNGs) untracked but still on disk -- safe to
  `rm -rf`, originals are on the NAS.
- Arrow-key prev/next on detail pages is global newest-first order, not the
  active filter's order (spec O1 accepted this for v1).

## Next Actions

1. **Owner: `npm run data`** when the NAS link is responsive (own terminal, no
   time pressure). Then:
   - un-gitignore `src/data/filter-index.json` + `src/data/details.json`
   - `git add src/data/*.json public/thumbnails public/detail && commit`
   - `rm -rf public/images data/raw/models.bootstrap.json`; delete
     `scripts/dev-bootstrap-raw.mjs`
   - paste the `npm run data` summary lines to sanity-check counts
2. **Step 8 -- Netlify branch deploy** for `feat/astro-rewrite` (needs step 1
   done so the committed snapshot exists). Verify the preview URL, incl. a
   detail-page round trip (`transition:persist` on mobile Safari -- degrade to
   plain nav if it regresses, per D3).
3. Verify grid scroll perf against the real snapshot on a throttled-CPU profile.
4. **Step 9 -- `/code-review`**, squash, merge to `main`, delete branch,
   repoint the production Netlify site.

Every module was built test-first (tests -> pass -> functional check). Coverage
per `## Test Coverage Standard` below.

## Test Coverage Standard

Defined 2026-09-01 with the v2.0 spec (`docs/astro-rewrite-spec.md` -> Testing). Runner: Vitest. Component tests: `@testing-library/preact`.

| Scope | Target | Status |
|---|---|---|
| `src/lib/**` (filter-engine, use-gallery, url-state, grid-layout, detail-paths) | 90% lines/branches | met (100 / ~96) |
| `scripts/lib/model-resolve.mjs` | 90% | met (100 / 95) |
| `scripts/lib/recency.cjs` | 85% | met |
| `scripts/build-filter-index.mjs` -- `buildIndex()` | 85% | met (`main()` fs glue exempt) |
| `src/components/GalleryIsland.tsx` | 70% | met (10 interaction tests) |
| `src/pages/m/[id].astro` `getStaticPaths` | smoke-tested via `detail-paths.ts` | met |
| Exempt | `scripts/scan-nas.mjs` + `make-thumbnails.mjs` (fs/NAS/sharp), `dev-bootstrap-raw.mjs` (throwaway), `astro.config.mjs`, `*.astro` pages | -- |

## Out of Spec

- `src/data/filter-index.json` + `details.json` temporarily gitignored (built
  from the bootstrap); committed once the real `npm run data` has run.
- `package.json` author field still empty.

## Deployment

Netlify builds via GitHub pull hook (push to `main` = auto-deploy). `npm run build` = `astro build` only -- it reads the committed `src/data/*.json` + `public/thumbnails|detail/` snapshot and never contacts the NAS, so the build environment needs nothing special. Refreshing the snapshot (`npm run data`) is a local, NAS-connected step whose outputs are committed.

Branch strategy: `feat/astro-rewrite` gets a Netlify **branch deploy** for preview; production stays on the old build until merge.

## Development

```bash
npm install

# no NAS needed unless refreshing the data snapshot
npm run dev              # astro dev
npm run build            # astro build -- reads the committed snapshot only
npm test                 # vitest run

# Refresh the data snapshot (requires NAS access; commit the outputs -- D7)
$env:ORYNT3D_DIR = "\\192.168.254.200\data\3D Files"   # only if the default IP changed
npm run data             # scan-nas -> make-thumbnails -> build-filter-index

# One-time, until the real scan has run: build src/data/*.json from the old snapshot
node scripts/dev-bootstrap-raw.mjs
node scripts/build-filter-index.mjs --raw data/raw/models.bootstrap.json --no-thumb-check
```
